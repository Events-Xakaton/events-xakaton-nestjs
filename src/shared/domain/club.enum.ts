export enum ClubMembershipStatus {
  Joined = 'joined',
  Left = 'left',
  Banned = 'banned',
}

export enum ClubMembershipRole {
  Owner = 'owner',
  Admin = 'admin',
  EventManager = 'event_manager',
  Member = 'member',
}
