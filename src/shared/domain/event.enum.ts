export enum EventStatus {
  Upcoming = 'upcoming',
  Ongoing = 'ongoing',
  Past = 'past',
  Cancelled = 'cancelled',
}

export enum EventParticipationStatus {
  Joined = 'joined',
  Left = 'left',
}
